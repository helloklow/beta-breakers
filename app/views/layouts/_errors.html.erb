<% if object.errors.any? %>
    <div id="error_explanation">
        <h4><%= pluralize(object.errors.count, "error") %> errors occurred: </h4>
        <% object.errors.full_messages.each do |msg| %>
            <em><%= msg %></em><br>
        <% end %><br>
    </div>
<% end %>s