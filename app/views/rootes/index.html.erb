<center>
<h1> All Routes </h1>

<% @rootes.each do |r| %>
    <%= link_to r.name, roote_path(r) %> - <%= r.category %> (<%= r.difficulty %>) 
    <%= button_to 'Edit', edit_roote_path(@roote), method: :get %>
    <%= button_to 'Delete', roote_path(@roote), method: :delete %>
    <br>
<% end %><br>

<%= link_to 'Home', user_path(@user) %>

</center>