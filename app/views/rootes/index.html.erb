<center>
<h1> All Beta </h1>

<% @rootes.each do |r| %>
    <%= link_to r.name, roote_path(r) %> - <%= r.category %> (<%= r.difficulty %>)<br>
<% end %><br>

<%= link_to 'Home', user_path(@user) %> | <%= link_to 'Logout', logout_path, method: :delete %>
</center>